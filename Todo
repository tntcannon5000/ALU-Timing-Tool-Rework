bundle the font in with the exe so that it works for all users
fix the finish detection issue that zeyan noticed (early finish detection in PL by about .5 seconds), potentially caused by 1 frame of lag where the VT updated but nothing else did (since VT server side)
fix failed finish detection that cdash observed (caused by wreck failsafe interacting with abnormally long server connection time at the finish line)
make it compatible with windows store and epic games versions
make a function to edit ghosts when the user accidentally messes them up somehow
make the way that users interact with ghosts and splits easier and more intuitive
make the ui always very clean and even, even when sliders aren't showing, etc
make it more compatible with people who can't run 60 fps
add back data logging to the exe
make UI config not appear in the ghost selection menu
add checkpoint and nitro %, make a nitro bar display that turns purple 0.2 seconds before full
try to see if it's possible to make a checkpoint bar to show progress between checkpoints
Try to find a better way to show half-width displays like vdelta, time gain ratio, and checkpoint(?), especially when there is an odd number of them
settings menu with hotkeys and color options, maybe move some of the checkboxes there, also include reaction time settings and ghost settings